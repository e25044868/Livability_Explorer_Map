# 生活機能探索地圖：頁面配置與互動規劃

版本：MVP v1｜更新日期：2026-08-01

## 1. 產品介面原則

1. 首屏先回答「這個區域符合多少生活需求」，地圖用來解釋結果，不是唯一主角。
2. 多類別為預設操作，不把使用者限制在單一設施搜尋。
3. 每個數字都能追溯資料來源、官方更新狀態與本站同步時間。
4. 缺座標、資料可能截斷或類別範圍有限時，介面明確揭露，不用錯誤位置或模糊名稱掩蓋。
5. 桌機採分析面板＋地圖；手機先選需求，再以摘要卡與 bottom sheet 呈現結果。

## 2. MVP 路由

| 路由 | 頁面 | 目的 | MVP |
|---|---|---|---|
| `/` | 生活圈探索 | 搜尋位置、選需求、看符合度與地圖 | 必做 |
| `/places/:publicId` | 設施詳情 | 看政府特有欄位、周邊機能與來源 | 必做 |
| `/sources` | 資料來源 | 透明呈現來源、同步、完整度與限制 | 必做 |
| `/compare` | 區域比較 | 比較兩個位置／生活圈 | 預留入口，v1 顯示即將推出 |
| `/about` | 使用說明 | 說明方法、距離、免責與回報方式 | 精簡版 |

## 3. 首頁資訊架構

### 3.1 桌機版（建議斷點 ≥ 1024px）

```text
┌──────────────────────────────────────────────────────────────────────────┐
│ Logo 生活機能探索地圖      [搜尋地址、地標或生活需求________] [定位] │
├──────────────────────────────┬───────────────────────────────────────────┤
│ 生活機能分析面板 360–420px   │ 地圖                                      │
│                              │                                           │
│ [一般][高齡][親子][駕車][急救]│  [搜尋此區域]                [圖層/定位] │
│                              │                                           │
│ 需求（可複選）               │       marker clusters / radius circle     │
│ ☑停車 ☑公廁 ☑AED □藥局      │                                           │
│ □醫療 □機車充電             │                                           │
│                              │                                           │
│ 半徑 [500m][1km][3km][範圍] │                                           │
│                              │                                           │
│ 符合度 4/6  缺少：醫療、充電 │                                           │
│ ┌各類數量／最近距離摘要────┐ │                                           │
│ └─────────────────────────┘ │                                           │
│ [摘要] [設施清單]           │                                           │
│ 結果卡 1                    │                                           │
│ 結果卡 2                    │                                           │
├──────────────────────────────┴───────────────────────────────────────────┤
│ 資料狀態：停車 261｜公廁目前可定位 841｜最後同步…｜查看資料來源       │
└──────────────────────────────────────────────────────────────────────────┘
```

桌機面板固定在左側，內部可捲動；地圖維持剩餘寬度。選中設施後，左側結果卡展開，地圖平移到標記但不強制縮放。面板可收合為 56px 工具列，讓小型筆電保留地圖空間。

### 3.2 手機版（< 768px）

```text
┌──────────────────────────────┐
│ 生活機能探索地圖      [來源] │
│ [搜尋地址、地標或生活需求__] │
│ [使用目前位置]               │
│ 情境：一般 高齡 親子 駕車 急救│
│ 需求：停車 公廁 AED 藥局 …  │
│ 半徑：500m  1km  3km         │
│ [分析這個生活圈]             │
├──────────────────────────────┤
│ 地圖（約 42–50vh）           │
│         [搜尋此區域]         │
├──────────────────────────────┤
│ Bottom sheet                 │
│ ─── 4/6 項符合・共 28 處     │
│ [摘要] [清單]                │
│ 停車 4・最近 120m            │
│ 公廁 3・最近 80m             │
└──────────────────────────────┘
```

手機初次進入時，bottom sheet 預設半高並先顯示需求選擇；完成分析後切換為摘要。使用者可拖曳至 25%、55%、90% 三個高度。選取結果卡時 sheet 升至 55%，保留部分地圖脈絡。

## 4. 首頁元件規格

### 4.1 搜尋與定位

- 搜尋提示：「搜尋地址、地標或生活需求」。
- 搜尋結果分組：地址／行政區、已匯入設施、類別捷徑。
- 定位狀態：未請求、請求中、成功、權限拒絕、逾時、不支援。
- 地址搜尋只取得中心點，不直接宣稱附近設施；取得中心點後才呼叫生活圈摘要。

### 4.2 情境模式

- 一般：不預選或沿用上次選擇。
- 高齡友善：預選公廁、藥局、醫療、AED。
- 親子友善：預選公廁、醫療、停車。
- 駕車需求：預選停車、機車充電；若未來有汽車充電需拆成獨立類別。
- 緊急醫療：預選 AED、醫療、藥局。
- v1 只提供預選組合，不顯示未經驗證的「友善分數」。

### 4.3 類別選擇

- 使用可複選 chip，不設「全部」與其他 chip 同時選中的矛盾狀態。
- 每個 chip 顯示圖示、名稱與當前範圍筆數；資料不可用時停用並顯示原因。
- 「機車充電」使用完整名稱，避免誤導汽車使用者。

### 4.4 生活機能摘要

摘要卡固定顯示：

- 選取需求符合數，例如 `4 / 6`。
- 各類設施數量、最近距離、最近設施名稱。
- 缺少的需求。
- 半徑與中心點描述。
- 資料完整度提示，例如「公廁有 159 筆缺少可信座標，未納入地圖計算」。

符合度只代表選取類別在範圍內是否至少有一處，不代表品質或安全評分。建議 v1 排序：符合類別數降冪 → 最近設施總距離升冪 → 資料完整度降冪。

### 4.5 地圖

- 類別使用固定顏色與圖示，但不能只靠顏色辨識。
- marker clustering 在縮小比例尺啟用；選定半徑顯示圓形範圍。
- 地圖移動後不自動查詢，顯示「搜尋此區域」，避免不受控 API 請求。
- viewport 過大時引導縮放，不顯示部分結果冒充完整結果。
- 沒有可信座標的設施不建立 marker。

### 4.6 結果卡

共用欄位：名稱、類別、距離、地址、開放時間（若有）、資料來源狀態。

類別特有欄位：

- 停車：汽車／機車／大車位、收費、型式、管理業者。
- 公廁：名稱、地址；若能從名稱解析男女／無障礙屬性，只標示為來源文字推定。
- AED：樓層或地點描述、市話。
- 藥局：電話；不顯示負責人姓名。
- 醫療：科別 chips、電話。
- 機車充電：計費方式、設置地點；顯示「不提供即時空位／接頭資訊」。

操作：`查看詳情`、`Google Maps 導航`、`回報資料錯誤`。

## 5. 設施詳情頁

由上而下：

1. 返回探索、類別標籤、名稱、距離與主要動作。
2. 小型地圖與地址；無可信座標時改顯示「位置待確認」，不畫假 marker。
3. 類別特有資訊區。
4. 500 公尺周邊機能摘要與最近設施。
5. 資料來源卡：提供機關、資料集、官方更新、本站同步、location accuracy、資料限制。
6. 回報錯誤：錯誤類型、說明；v1 可先連結至表單或 mailto，不建立帳號系統。

桌機採 8/4 欄：主內容在左，導航與來源卡 sticky 在右。手機採單欄，主要動作固定在底部安全區上方。

## 6. 資料來源頁

- 首屏說明本站不是政府官方網站，以及「官方更新」與「本站同步」的差異。
- 每個資料集一張狀態卡：類別、提供機關、端點狀態、本站有效筆數、座標覆蓋率、最後成功同步、授權、已知限制。
- 對公廁顯示「本次回應 1,000 筆，其中 841 筆具可信座標」；不能寫成全高雄共有 1,000 處。
- 對醫療顯示「API 回應疑似有 1,000 筆上限，完整性確認中」。
- 對機車充電明確說明不代表汽車充電站，也沒有即時狀態。

## 7. 關鍵互動流程

### 流程 A：分析目前位置

首頁 → 使用目前位置 → 選情境／類別 → 選半徑 → 呼叫摘要＋設施查詢 → 顯示符合度、缺項、清單與地圖。

### 流程 B：搜尋地址後分析

輸入地址 → 選搜尋結果 → 地圖定位中心 → 尚未分析提示 → 選需求 → 分析生活圈。

### 流程 C：搜尋目前地圖範圍

拖曳／縮放地圖 → 顯示搜尋此區域 → 檢查 viewport 面積 → 查詢 → 更新摘要與結果。若範圍過大，要求縮小。

### 流程 D：從設施反查周邊

選 marker／結果卡 → 設施詳情 → 查看 500 公尺周邊摘要 → 可將設施設為新的生活圈中心。

## 8. Loading、Empty、Error 與資料品質狀態

- Loading：地圖 skeleton＋摘要占位；保留上一輪結果但加上更新中遮罩。
- Empty：區分「範圍內沒有資料」與「此類資料尚無可信座標」。
- Partial：API 達回傳上限或有未定位資料，顯示醒目但不阻斷的資料完整度提示。
- Error：統一顯示 request ID、重試與回到上一結果；不顯示 stack trace。
- Offline：保留基本頁面與最後一次畫面狀態，但不宣稱結果為最新。
- 定位拒絕：提供地址搜尋與「以高雄車站為示範中心」等明確替代操作；示範中心不得冒充使用者位置。

## 9. API 與頁面對應

| 畫面需求 | API | 備註 |
|---|---|---|
| 地圖／清單結果 | `GET /api/places` | viewport 或中心點＋半徑；limit ≤ 500 |
| 生活機能摘要 | `GET /api/nearby-summary` | 同一中心與半徑 |
| 設施詳情 | `GET /api/places/{public_id}` | 不回傳內部 ID／raw_data |
| 類別與可用狀態 | `GET /api/categories` | 建議帶可定位筆數與同步狀態 |
| 搜尋 | `GET /api/search` | 名稱、地址、行政區、類別 |
| 資料來源頁 | 建議新增 `GET /api/data-sources` | 公開安全的來源與品質摘要 |

為避免摘要與地圖數字在兩次請求間不一致，兩個 API 應回傳相同的 `data_version` 或 `snapshot_at`。

## 10. 視覺與無障礙基線

- 字級：內文至少 16px；觸控目標至少 44×44px。
- 色彩對比符合 WCAG AA；marker 與圖例同時使用圖示、形狀與文字。
- 地圖不是唯一資訊來源，所有結果可由清單與摘要取得。
- 鍵盤可操作搜尋、類別、結果卡與詳情；焦點移動順序符合視覺順序。
- 螢幕閱讀器讀出距離、類別、資料狀態，但不逐一朗讀所有地圖 marker。
- 尊重 `prefers-reduced-motion`，bottom sheet 與地圖動畫可減少。

## 11. 開發切片

1. 建立無資料的響應式頁面骨架與狀態元件。
2. 串接停車場與公廁 API，完成摘要、清單、地圖與詳情。
3. 加入搜尋、定位、搜尋此區域與 viewport 防護。
4. 加入 AED／藥局地理編碼後的第三、第四類。
5. 完成資料來源頁、partial 狀態與來源透明度。
6. 最後才加入情境預選與複合符合度排序；不在資料不足時先做虛假分數。

## 12. 驗收重點

- 首頁 10 秒內能理解「分析生活圈」而非一般店家搜尋。
- 桌機與手機都能在三步內完成：選中心 → 選需求 → 看摘要。
- 多類別選擇、半徑、地圖與清單的結果一致。
- 公廁無效座標、醫療疑似截斷、機車充電範圍均在介面正確揭露。
- 地圖查詢沒有任何條件時不送出；過大 viewport 被前後端共同阻擋。
- 所有功能不依賴地圖滑鼠操作，也能透過鍵盤與清單完成。
